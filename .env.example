# Power CV - Docker Compose Environment Variables
# Copy this file to .env and update the values as needed

# ============================================
# Database Configuration (PostgreSQL)
# ============================================

POSTGRES_USER=power_cv_user
POSTGRES_PASSWORD=power_cv_password
POSTGRES_DB=power_cv

# Database URL for server connection
DATABASE_URL=postgresql://power_cv_user:power_cv_password@postgres:5432/power_cv

# ============================================
# Server Configuration
# ============================================

NODE_ENV=production
PORT=5001

# CORS Configuration - Update for production
CORS_ORIGIN=http://localhost:5173

# ============================================
# Client Configuration
# ============================================

# API URL for client to connect to backend
VITE_API_URL=http://localhost:5001/api

# ============================================
# pgAdmin Configuration (Optional)
# ============================================

PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin

# ============================================
# Production Settings (Override for production)
# ============================================

# Uncomment and update for production deployment:
# NODE_ENV=production
# CORS_ORIGIN=https://your-production-domain.com
# VITE_API_URL=https://api.your-production-domain.com
# POSTGRES_PASSWORD=your-secure-password-here
# PGADMIN_DEFAULT_PASSWORD=your-secure-password-here

# ============================================
# Notes
# ============================================

# 1. Never commit .env file to version control
# 2. Use strong passwords in production
# 3. Update CORS_ORIGIN to your production domain
# 4. Consider using Docker secrets for production
# 5. DATABASE_URL format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
